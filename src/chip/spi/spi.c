#include <stdint.h>
#include <stdio.h>

#include "spi.h"

void spi_init(void)
{
    printf("spi init.\n");

}

void spi_read(uint8_t size, uint8_t * data)
{
    (void) size;
    (void) data;

}

void spi_write(uint8_t size, uint8_t * data)
{
    (void) size;
    (void) data;
}